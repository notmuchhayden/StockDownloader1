<Window x:Class="StockDownloader1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockDownloader1"
        mc:Ignorable="d"
        Title="주식 과거 데이터 다운로드" Height="450" Width="800">
    <Grid>
        <Menu VerticalAlignment="Top">
            <MenuItem Header="_파일">
                <MenuItem Header="_열기"/>
                <MenuItem Header="_저장"/>
                <Separator/>
                <MenuItem Header="_종료" Click="ExitMenuItem_Click"/>
            </MenuItem>
        </Menu>
        <ToolBarTray VerticalAlignment="Top" Margin="0,25,0,0">
            <ToolBar>
                <Button Content="버튼1" Width="70" Margin="2"/>
                <Button Content="버튼2" Width="70" Margin="2"/>
                <Button Content="버튼3" Width="70" Margin="2"/>
            </ToolBar>
        </ToolBarTray>
        <Grid Margin="0,60,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <!-- 왼쪽: 목록 -->
            <ListBox Grid.Column="0"
                     ItemsSource="{Binding ItemList}"
                     SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                     DisplayMemberPath="Name"
                     Margin="5"/>
            <!-- 가운데: GridSplitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="LightGray"
                          ShowsPreview="True"/>
            <!-- 오른쪽: 상세 뷰 -->
            <Border Grid.Column="2" Margin="5" Padding="10" BorderBrush="Gray" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="상세 정보" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding SelectedItem.Description}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
